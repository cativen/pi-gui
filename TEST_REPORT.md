# Pi GUI 功能测试报告

- **版本**：1.0.0
- **日期**：2026-09-11
- **产物**：`build/distributions/pi-gui-1.0.0.zip` (687 KB)
- **测试范围**：含新增的 Edits / git diff、图片粘贴、`/` 命令补全（含 pi 内置命令）、右键菜单可达性
- **测试环境**：macOS (darwin 24.5.0)、JDK 17、IntelliJ IDEA 2026.1 (IU-261.22158.277)、pi CLI 0.84.2、Node 22.22.0

## 结论

自动化测试 **238 项全部通过**；JetBrains Plugin Verifier 对本机 IDEA 2026.1 判定 **Compatible**，无兼容性问题。

本轮排查发现 **2 个真实缺陷**（1 个功能性、1 个性能），均已修复并补上回归测试。另清理 1 处无效代码。

| 缺陷 | 严重度 | 状态 |
|---|---|---|
| 输入框粘贴文本完全失效 | 高（核心交互不可用） | ✅ 已修复 |
| 大内容渲染阻塞 UI，长会话打开卡顿约 1 秒 | 中（可用但明显卡顿） | ✅ 已修复 |
| 设置弹窗 `setOKButtonText` 为无效表达式 | 低（无用户影响） | ✅ 已清理 |
| 图片粘贴在真实 IDE 中不生效（绑在按键上，被 IDE 的 `$Paste` 抢先消费） | 高（功能形同虚设） | ✅ 已修复 |
| `/` 命令补全曾把附件 `@path` 拼在命令前面，导致 pi 不再识别为命令 | 高（命令静默退化成普通文字） | ✅ 已修复 |
| 命令列表沿用平台 `ColoredListCellRenderer`，忽略插件自己的深/浅色设置 | 中（强制浅色 + 深色 IDE 下不可读） | ✅ 已修复 |
| Esc 键被无条件绑定，会吞掉 IDE 自身的 Esc（返回编辑器） | 中（回归了原有快捷键） | ✅ 已修复 |
| 「Send File Path to Pi GUI」用 `anchor="last"`，在项目树右键菜单里被挤出屏幕，实际点不到 | 高（功能等于不存在） | ✅ 已修复 |
| `/session` 按文档的 `messageCount` 取值，而 pi 实际返回 `totalMessages` 等字段，消息数永远不显示 | 中（信息缺失） | ✅ 已修复 |
| `/tree` 按层级缩进，而会话本身是一条链，232 条消息会缩进 232 层 | 中（输出不可读） | ✅ 已修复 |

---

## 一、发现并修复的缺陷

### 缺陷 1：粘贴文本失效（高）

**现象**：在聊天输入框按 Cmd+V 粘贴文本，没有任何反应。复制（Cmd+C）同样受影响。

**根因**：实现拖拽上传附件时，我把 `input.transferHandler` 整个替换成了只认文件的处理器。Swing 的 `JTextComponent.paste()` / `copy()` / `cut()` 全部经由 `TransferHandler` 分发，替换后文本剪贴板操作被静默吞掉——不报错，只是没反应，所以之前没被发现。

**修复**：改为包装原处理器（`FileDropTransferHandler`）：文件类型自己处理，其余（含全部剪贴板流量）委派回文本框原本的处理器。`getSourceActions` / `exportToClipboard` / `exportAsDrag` 一并委派，复制和拖出也保持正常。

**验证**：

| 测试 | 结果 |
|---|---|
| 粘贴文本 → 内容进入输入框 | ✅ |
| 从输入框复制 → 剪贴板拿到内容 | ✅ |
| 拖入文件 → 变成附件 | ✅ |

修复前实测输出为空字符串，修复后为 `pasted text`。

### 缺陷 2：大内容阻塞 UI（中）

**现象**：打开消息多的会话、或遇到超大工具输出/代码块时，界面卡顿数秒。

**根因**：三处叠加

1. 折叠区块（思考块、工具调用、工具结果）**即使折叠也会立即构建内容**，白白付出词法高亮和 HTML 排版成本；
2. 内容**没有上限**，agent 动辄输出上万行；
3. 打开会话时**一次性渲染全部消息**，每条都要走一次 HTML 排版。

**修复**

1. `CollapsibleSection` 改为接收内容工厂，首次展开才构建；
2. `CodeBlock` 与消息渲染对超长内容截断（800 行 / 120 KB），并标注「已截断，共 N 行」——**复制按钮仍给出完整原文**；
3. 聊天记录**分页渲染**：只渲染最近 60 条，上方提供「加载更早的 N 条消息」。

**实测（同一台机器，同一份真实会话）**

| 场景 | 修复前 | 修复后 | 提升 |
|---|---|---|---|
| 打开 362 条消息的真实会话 | 987 ms | **17 ms** | 58× |
| 10000 行工具结果（折叠态） | 335 ms | **3 ms** | 112× |
| 5000 行代码块 | 3352 ms | **689 ms** | 4.9× |

用于测量的是你机器上真实的会话文件（`~/.pi/agent/sessions/`，1016 KB / 362 条消息）。

### 缺陷 3：`/` 命令与附件拼接顺序冲突（高）

**现象**：先添加一个文件附件、再输入 `/review`，pi 收到的是 `@src/App.kt /review`——不再以 `/` 开头，命令退化成普通文字发给模型。

**根因**：原有 `send()` 一律把 `@path` 提到最前面。而 pi 的 `expandPromptTemplate` 用 `^/([^\s]+)(?:\s+[\s\S]*)?$` 匹配**整条消息**，命令必须在偏移 0。

**修复**：抽出 `ChatPanel.composeMessage()`——文本以 `/` 开头时命令在前、`@path` 作为参数跟在后面；其余情况维持原顺序。发送路径和测试共用同一个函数，避免测试通过但实现走样。

### 缺陷 4：命令列表不跟随插件的深浅色设置（中）

**现象**：离屏渲染深色主题时，命令列表仍是浅灰底 + 深色文字。

**根因**：`ColoredListCellRenderer` 和 `SimpleTextAttributes` 都从 IDE 的 LaF 取色，绕过了插件自己的 `PiTheme`。用户可以把聊天面板锁定为浅色/深色，与 IDE 无关——这正是 `PiTheme` 存在的理由。

**修复**：换成自绘的 `ListCellRenderer`（两个 `JLabel`，全部取色于 `PiTheme`），并在每次弹出时重新读取调色板。长描述交给 `JLabel` 自动省略号截断。

### 缺陷 5：Esc 被无条件吞掉（中）

**现象**：给补全列表绑定 Esc 后，列表未打开时在输入框按 Esc 也不再返回编辑器。

**根因**：只要 `InputMap` 有绑定且 action 处于 enabled，Swing 就会消费该按键。

**修复**：`overrideKey` 的 action 覆写 `isEnabled()`——列表未打开且没有原生 action 可委派时返回 false，Swing 便不消费事件。已加回归测试断言 `pi.command.cancel` 在列表关闭时 `isEnabled == false`。


### 缺陷 6：右键菜单里的「Send File Path to Pi GUI」点不到（高）

**现象**：在项目树里右键文件或模块，看不到「Send File Path to Pi GUI」；但在编辑器标签页右键能看到。

**根因**：`plugin.xml` 里用的是 `anchor="last"`。`ProjectViewPopupMenu` 在装了 Maven / Arthas / Apifox / GitLab 等插件后长达四五十项，菜单底部已经超出屏幕——**菜单项确实注册了，只是永远滚不到**。编辑器标签页菜单较短，所以那里能看到。

**修复**：五个菜单组全部改为 `anchor="first"`，并补上导航栏（`NavbarPopupMenu`）与作用域视图（`ScopeViewPopupMenu`），覆盖「任何文件或文件夹」。新增测试断言该项在每个菜单组中的下标 ≤ 2，位置回退会立刻失败。

### 缺陷 7：`/session` 读了文档里的字段名（中）

**现象**：`/session` 不显示消息条数。

**根因**：RPC 文档示例写的是 `messageCount`，**真实 CLI 返回的是** `totalMessages` / `userMessages` / `assistantMessages` / `toolCalls` / `toolResults`。这是继 `sourceInfo` 之后，文档与实现第二次不一致。

**修复**：改读真实字段并展示细分（`232 (3 from you, 103 from pi, 127 tool calls)`），同时保留对 `messageCount` 的兼容。测试固件直接取自真实会话的实测响应。

### 缺陷 8：`/tree` 缩进爆炸（中）

**现象**：一条 232 条消息的真实会话，`/tree` 输出缩进 232 层，完全没法看。

**根因**：会话树在没有分叉时就是一条**链**——每条消息都是上一条的唯一子节点。按层级缩进等于按消息数缩进。

**修复**：只在**真正的分叉点**增加缩进；同时过滤掉 toolResult 和纯工具调用的 assistant 条目（对"会话走到哪了"毫无信息量），并限制 200 行。同一份真实会话从 232 行噪音变成 44 行可读提纲，且中间那个真实存在的分叉点被正确标了出来。


---

## 二、逐功能验证

### 1. 会话管理

| 项 | 方式 | 结果 |
|---|---|---|
| 扫描会话目录 | 真实 `~/.pi/agent/sessions/` | ✅ 12 个会话全部识别 |
| 解析 JSONL | 真实文件，含 362 条消息的大会话 | ✅ 33 ms 解析完成 |
| 仅显示活动分支 | 构造分叉会话 | ✅ 废弃分支不显示 |
| parentId 成环不死循环 | 构造环 | ✅ |
| 损坏行跳过 | 混入非 JSON 行 | ✅ |
| 标题回退（name → 首条用户消息） | 真实+构造 | ✅ 中文标题正常 |
| 压缩条目转为提示 | 构造 | ✅ |

### 2. 与 pi 的 RPC 通信

对**真实 pi CLI** 实测，6 个命令全部返回 `success: true`：

`get_state` · `get_available_models` · `get_available_thinking_levels` · `get_messages` · `get_commands` · `get_session_stats`

| 项 | 结果 |
|---|---|
| 事件序列（agent_start → message_start/update/end → agent_settled） | ✅ 与实现一致 |
| 流式增量累积（text/thinking/toolcall 三类 delta） | ✅ 9 项单测 |
| 工具调用两种拼写归一化（会话文件 vs 线协议） | ✅ |
| 图片附件线格式 | ✅ 真机实测，pi 回显结构与发送一致 |
| 进程退出、错误响应处理 | ✅ |

### 3. 渲染

| 项 | 结果 |
|---|---|
| Markdown（标题/列表/表格/引用/行内格式/链接） | ✅ 15 项单测 |
| 代码围栏拆分、未闭合围栏 | ✅ |
| HTML 转义（防注入） | ✅ |
| 语法高亮（IDE 词法器） | ✅ XML 8 个色块、SQL 5 个色块 |
| 缩进保留 / 行内空格可折行 | ✅ |
| 中文（CJK）渲染 | ✅ 截图确认 |
| 深浅色对比度 | ✅ 断言亮度阈值 |

### 4. 设置

| 项 | 结果 |
|---|---|
| 外观三选（跟随系统/浅色/深色） | ✅ 强制模式下背景与文字颜色同时翻转 |
| 聊天字体大小滑块（10–24px，0=跟随 IDE） | ✅ 含越界钳制 |
| 默认展开思考块 | ✅ |
| 语言三选，默认简体中文 | ✅ |
| 设置往返（reset/apply/isModified） | ✅ |
| 变更通知触发界面刷新 | ✅ |

### 5. 国际化

| 项 | 结果 |
|---|---|
| 三种语言文案互不相同 | ✅ |
| 简中/繁中确为翻译（非英文兜底） | ✅ |
| **三份文件 key 完全一致** | ✅ 有专门断言，防漏翻 |
| 占位符替换 | ✅ |
| 缺失 key 回退英文 → 再回退 key 本身 | ✅ |

### 6. 技能

| 项 | 方式 | 结果 |
|---|---|---|
| 列出全局技能 | 真实 `~/.agents/skills/` | ✅ 4 个全部识别，与你截图一致 |
| 启用/禁用（改 `disable-model-invocation`） | 临时文件 | ✅ |
| **禁用再启用后文件逐字节还原** | 临时文件 | ✅ 关键安全属性 |
| 正文/格式/CRLF 保留 | 临时文件 | ✅ |
| skills.sh 搜索 | 真实 API | ✅ HTTP 200，1.4s |

### 7. 插件（包）

| 项 | 方式 | 结果 |
|---|---|---|
| 读取已安装包（字符串式/对象式条目） | 构造 settings.json | ✅ |
| 损坏 settings 不抛异常 | 构造 | ✅ |
| pi.dev 搜索解析 | **真实页面存为测试固件** | ✅ 解出 50 个包 |
| scoped 包名（`@scope/name`）不被破坏 | 真实固件 | ✅ |
| 站点改版时优雅降级 | 构造异常 HTML | ✅ 返回空列表而非崩溃 |
| pi.dev 可达性 | 真实请求 | ✅ HTTP 200，2.1s |

### 8. `/` 命令补全

| 项 | 方式 | 结果 |
|---|---|---|
| 命令列表来源 `get_commands` | **真实 pi CLI 0.84.2** | ✅ 返回 extension / skill 两类共 5 条 |
| 响应解析（文档的扁平 `path`/`location` 与实际的嵌套 `sourceInfo`） | 真实响应存为测试固件 | ✅ 两种结构都能解析 |
| 触发规则：仅在消息开头的 `/` | 单测 | ✅ `and/or`、URL、前导空格都不触发 |
| 光标移入参数后关闭 | 单测 | ✅ |
| 过滤（前缀、忽略 `skill:` 前缀、大小写、排序） | 单测 | ✅ 输入 `/xlsx` 能找到 `skill:xlsx` |
| 选中后插入 `/name `，保留已输入的参数 | 单测 | ✅ |
| 无匹配时仍显示「没有匹配的 pi 命令」而非静默关闭 | 单测 | ✅ |
| 无 agent 运行时的离线探测（一次性 `pi --mode rpc`） | **真实 CLI** | ✅ 约 1.5s 返回 |
| 探测不写入会话文件 | **真实 CLI** | ✅ 只创建空目录（pi 每次启动都会建），无 `.jsonl` |
| 深/浅色渲染 | 离屏 PNG | ✅ 两种主题均正确（见 `/tmp/pi-gui-commands-*.png`） |
| 未打开时 Esc / 上下键 / Tab 行为不变 | 单测 | ✅ |
| 内置命令表与 pi 的 `BUILTIN_SLASH_COMMANDS` 完全一致（23 条、顺序相同） | 单测 | ✅ 漂移即报警 |
| 内置命令在探测返回前就可用 | 单测 | ✅ 输入 `/` 立刻有 23 条 |
| 扩展命令不会顶掉同名内置命令（与 pi 行为一致） | 单测 | ✅ |
| 内置命令不会被当成 prompt 发给模型 | 单测 | ✅ CLI-only 的 7 条逐条断言 |
| `/session` 渲染 | **真实 CLI 响应** | ✅ 消息数/token/成本/上下文四项齐全 |
| `/tree` 渲染 | **真实 232 条会话** | ✅ 44 行提纲，分叉点与当前位置标出 |
| `/copy`、`/export` 线协议 | **真实 CLI** | ✅ `get_last_assistant_text`、`export_html` 均返回成功 |
| `/hotkeys` 的 keymap 配置页 id | 平台 jar 校验 | ✅ `preferences.keymap` 真实存在 |

### 9. 其他

| 项 | 结果 |
|---|---|
| Git 分支（含 worktree、detached、嵌套目录、带斜杠分支名） | ✅ 真实仓库实测：`master` / `dev` / 非仓库返回 null |
| Git diff（编辑文件识别 + HEAD 对比） | ✅ 13 项单测 + 真实仓库实测 |
| 缓存命中率计算（output 不参与分母） | ✅ 真实数据验证指标非零 |
| 耗时统计（实时计秒 + 历史时间戳推算） | ✅ 排除工具执行时间 |
| 发送路径右键菜单（文件/文件夹/多选/选区） | ✅ 7 项测试，含 plugin.xml 注册断言 |
| 附件（图片 base64 / 其他转 @path、10 张 10MB 上限） | ✅ 10 项测试 |
| 图片粘贴（走 TransferHandler，覆盖 IDE action / Swing paste / 拖拽） | ✅ 5 项测试 + 真实 macOS 剪贴板实测 |
| `/` 命令补全（触发规则、过滤、插入、按键、离线探测） | ✅ 26 项测试 + 真实 pi CLI 实测 |
| pi 内置命令（23 条，对齐 pi 的 `BUILTIN_SLASH_COMMANDS`） | ✅ 20 项测试 + 真实 CLI 负载实测 |
| 右键菜单位置（5 个菜单组，断言排在前 3 位） | ✅ 1 项测试 |

---

## 三、未能验证的部分

诚实说明以下几点**没有做到端到端实测**：

1. **真实模型对话未跑通**。你的两个 provider 目前都不可用——`zai-coding-cn` 用量超限、`anthropic` 的 key 返回 401。因此流式增量的**视觉效果**（逐字显示、光标动画）我只用符合协议的合成事件做了单测，没有真跑过一次完整回答。协议格式本身已用真实 CLI 验证。

2. **技能安装 / 插件安装未执行**。这两个操作会在你机器上下载并执行第三方代码，不是验证该做的事。我只验证了命令构造、确认流程和搜索链路。

3. **图标渲染无法离屏验证**。折叠箭头、附件按钮等 `AllIcons` 图标在无头环境不绘制，截图里看不见。实际 IDE 中应正常，但这条只能由你确认。

4. **`/` 命令的真实按键路径未在 IDE 中实测**。补全的触发、过滤、插入、按键启用状态都有单测覆盖，弹窗外观用离屏 PNG 确认，但「在真实 IDE 里按下 Cmd/方向键时弹窗确实出现」这一步只能由你确认——无头测试环境无法让弹窗真正上屏（`locationOnScreen` 需要窗口已显示）。上一轮图片粘贴的教训已吸取：这次按键没有走 `InputMap` 之外的自定义分发，Enter 沿用的是本来就能工作的 `pi.send` 绑定。

5. **pi.dev 搜索依赖 HTML 抓取**。该站无 JSON API（`/api` 路由返回 501），只能解析页面结构化属性。**站点改版即会失效**——所以手写 Source 输入框始终可用，且已有测试在改版时立即报警。

---

## 四、已知限制（非缺陷）

- 超过 800 行 / 120 KB 的内容在界面上截断显示，复制仍为完整内容。
- 会话超过 60 条消息时默认只渲染最近部分，需点击加载更早消息。
- `FileChooserDescriptorFactory.createSingleFileDescriptor()` 有 1 处 deprecated 调用（Verifier 报告，非阻塞）。
- **项目级 prompt 模板（`.pi/prompts/*.md`）在 RPC 模式下不会出现在命令列表里**。这是 pi 自身的行为（文档注明「仅在项目被信任后」加载），实测无论在临时目录还是本项目目录都取不到；全局的 `~/.pi/agent/prompts/*.md` 则正常出现。插件只如实展示 `get_commands` 的返回，没有绕过。
- 首次按 `/` 时，23 条内置命令**立刻**出现；扩展/技能命令需要一次性 pi 进程探测（约 1.5 秒），期间列表底部显示「正在加载 pi 命令…」。发出第一条消息后改由常驻 agent 提供，不再有延迟。
- **`/tree` 只能看，不能切分支**。pi 的 RPC 没有任何切换活动分支的命令（`get_tree` 是只读的，`switch_session` 换的是文件不是分支），所以插件能显示会话树的形状和当前位置，但做不到 TUI 里那种切换。要切分支请用 `pi` 终端，或用 `/fork` 从某条历史消息开新分支。
- **7 条内置命令需要 pi 的终端界面**：`/login`、`/logout`、`/trust`、`/share`、`/import`、`/changelog`、`/scoped-models`。它们涉及认证流程、信任决策或交互式选择器，RPC 没有对应命令。选中它们会明确提示去终端执行，而不是把这行字发给模型。
- 已安装包的资源统计只能统计**显式声明**了 extensions/skills 等过滤字段的条目；字符串式条目会自动加载全部内容，其数量在配置文件里无从得知。

---

## 五、复现方式

```bash
cd /Users/zhoutao/code/pi-gui
./gradlew clean test          # 238 项测试
./gradlew verifyPlugin        # JetBrains 官方兼容性校验
./gradlew buildPlugin         # 产出 zip
```

渲染效果可离屏导出为图片自行查看：

```bash
./gradlew test --tests '*PreviewRenderer*'
```

产物位于 `/tmp/pi-gui-preview-dark.png` 与 `/tmp/pi-gui-preview-light.png`。

`/` 命令弹窗的外观单独渲染：

```bash
./gradlew test --tests '*CommandPopupPreview*'
```

产物位于 `/tmp/pi-gui-commands-dark.png` 与 `/tmp/pi-gui-commands-light.png`。
